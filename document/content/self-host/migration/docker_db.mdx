---
title: Docker 数据库迁移(无脑操作)
description: gptGO Docker 数据库备份和迁移
---

## 1. 停止服务

```bash
docker-compose down
```

## 2. Copy 文件夹

Docker 部署数据库都会通过 volume 挂载本地的目录进入容器，如果要迁移，直接复制这些目录即可。

`PG 数据` : pg/data
`Mongo 数据` : mongo/data

直接把 pg 和 mongo 目录全部复制走即可。
